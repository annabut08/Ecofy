{
    "builds": [
        {
            "src": "backend/src/main.py",
            "use": "@vercel/python",
            "config": {
                "requirementsTxt": "requirements.txt"
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "backend/src/main.py"
        }
    ]
}